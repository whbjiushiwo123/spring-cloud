server:
  port: 5000
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: file:\\\E:\github\configs
          force-pull: true
management:
  endpoints:
    web:
      exposure:
        include: "*"